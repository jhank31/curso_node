// forma tradicional

function sumar ( a, b ) {
    return a + b;
}

// funcion de flecha

const restar = ( a, b ) => a - b;
const saludar = () => 'Hola Mundo';

console.log( restar(14, 5));
console.log( saludar());